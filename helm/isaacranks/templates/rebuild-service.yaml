apiVersion: v1
kind: Service
metadata:
  name: {{ template "isaacranks.rebuild" . }}
  labels:
    app: {{ template "isaacranks.fullname" . }}
    chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    release: {{ .Release.Name | quote }}
    heritage: {{ .Release.Service | quote }}
    component: rebuild
spec:
  type: ClusterIP
  ports:
    - name: web
      port: 80
      targetPort: 3000
      protocol: TCP
  selector:
    app: {{ template "isaacranks.fullname" . }}
    component: rebuild
    release: {{ .Release.Name | quote }}
